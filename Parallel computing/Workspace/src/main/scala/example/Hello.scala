package example




object Hello extends  App {

  /*
  private var uidCount = 0L
  def getUniqueID: Long = {
    uidCount = uidCount + 1
    uidCount
  }

  def startHilo() = {
    val t = new Thread {
      override def run() {
        val uids = for( i <- 0 until 100) yield getUniqueID
        println(uids)
      }
    }
    t.start()
    t
  }

  startHilo();startHilo()
  */
  println("ok")
}
